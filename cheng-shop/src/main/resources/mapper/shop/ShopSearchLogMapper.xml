<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheng.shop.mapper.ShopSearchLogMapper">

    <resultMap type="com.cheng.shop.domain.ShopSearchLog" id="ShopSearchLogResult">
        <id     property="logId"       column="log_id"/>
        <result property="memberId"    column="member_id"/>
        <result property="guestId"     column="guest_id"/>
        <result property="keyword"     column="keyword"/>
        <result property="resultCount" column="result_count"/>
        <result property="createTime"  column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.cheng.shop.domain.ShopSearchLog" useGeneratedKeys="true" keyProperty="logId">
        insert into shop_search_logs (member_id, guest_id, keyword, result_count, create_time)
        values (#{memberId}, #{guestId}, #{keyword}, #{resultCount}, now())
    </insert>

    <select id="selectPopularKeywords" resultType="com.cheng.shop.domain.vo.PopularSearchVO">
        select keyword,
               count(*) as searchCount
        from shop_search_logs
        where create_time &gt;= #{since}
        group by keyword
        order by searchCount desc
        limit #{limit}
    </select>

    <update id="mergeGuestToMember">
        UPDATE shop_search_logs
        SET member_id = #{memberId}, guest_id = NULL
        WHERE guest_id = #{guestId}
    </update>

</mapper>
