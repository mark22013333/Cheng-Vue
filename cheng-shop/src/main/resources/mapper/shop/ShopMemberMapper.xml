<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheng.shop.mapper.ShopMemberMapper">

    <resultMap type="com.cheng.shop.domain.ShopMember" id="ShopMemberResult">
        <id     property="memberId"     column="member_id"/>
        <result property="memberNo"     column="member_no"/>
        <result property="nickname"     column="nickname"/>
        <result property="avatar"       column="avatar"/>
        <result property="mobile"       column="mobile"/>
        <result property="email"        column="email"/>
        <result property="password"     column="password"/>
        <result property="gender"       column="gender"/>
        <result property="birthday"     column="birthday"/>
        <result property="points"       column="points"/>
        <result property="level"        column="level"/>
        <result property="status"       column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp"  column="last_login_ip"/>
        <result property="createTime"   column="create_time"/>
        <result property="updateTime"   column="update_time"/>
    </resultMap>

    <sql id="selectMemberVo">
        select member_id, member_no, nickname, avatar, mobile, email, password, gender, birthday,
               points, level, status, last_login_time, last_login_ip, create_time, update_time
        from shop_member
    </sql>

    <select id="selectMemberList" parameterType="com.cheng.shop.domain.ShopMember" resultMap="ShopMemberResult">
        <include refid="selectMemberVo"/>
        <where>
            <if test="nickname != null and nickname != ''">
                AND nickname like concat('%', #{nickname}, '%')
            </if>
            <if test="mobile != null and mobile != ''">
                AND mobile like concat('%', #{mobile}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email like concat('%', #{email}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="level != null">
                AND level = #{level}
            </if>
        </where>
        order by member_id desc
    </select>

    <select id="selectMemberById" parameterType="Long" resultMap="ShopMemberResult">
        <include refid="selectMemberVo"/>
        where member_id = #{memberId}
    </select>

    <select id="selectMemberByMobile" parameterType="String" resultMap="ShopMemberResult">
        <include refid="selectMemberVo"/>
        where mobile = #{mobile}
    </select>

    <select id="selectMemberByEmail" parameterType="String" resultMap="ShopMemberResult">
        <include refid="selectMemberVo"/>
        where email = #{email}
    </select>

    <insert id="insertMember" parameterType="com.cheng.shop.domain.ShopMember" useGeneratedKeys="true" keyProperty="memberId">
        insert into shop_member
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="nickname != null and nickname != ''">nickname,</if>
            <if test="avatar != null">avatar,</if>
            <if test="memberNo != null and memberNo != ''">member_no,</if>
            <if test="mobile != null">mobile,</if>
            <if test="email != null">email,</if>
            <if test="password != null and password != ''">password,</if>
            <if test="gender != null">gender,</if>
            <if test="birthday != null">birthday,</if>
            <if test="points != null">points,</if>
            <if test="level != null">level,</if>
            <if test="status != null and status != ''">status,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="nickname != null and nickname != ''">#{nickname},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="memberNo != null and memberNo != ''">#{memberNo},</if>
            <if test="mobile != null">#{mobile},</if>
            <if test="email != null">#{email},</if>
            <if test="password != null and password != ''">#{password},</if>
            <if test="gender != null">#{gender},</if>
            <if test="birthday != null">#{birthday},</if>
            <if test="points != null">#{points},</if>
            <if test="level != null">#{level},</if>
            <if test="status != null and status != ''">#{status},</if>
            now()
        </trim>
    </insert>

    <update id="updateMember" parameterType="com.cheng.shop.domain.ShopMember">
        update shop_member
        <trim prefix="SET" suffixOverrides=",">
            <if test="nickname != null and nickname != ''">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="mobile != null">mobile = #{mobile},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="points != null">points = #{points},</if>
            <if test="level != null">level = #{level},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
            update_time = now()
        </trim>
        where member_id = #{memberId}
    </update>

    <delete id="deleteMemberById" parameterType="Long">
        delete from shop_member where member_id = #{memberId}
    </delete>

    <update id="increasePoints">
        update shop_member set points = points + #{points} where member_id = #{memberId}
    </update>

    <update id="decreasePoints">
        update shop_member set points = points - #{points} where member_id = #{memberId} and points >= #{points}
    </update>

    <select id="checkMobileUnique" resultType="int">
        select count(*) from shop_member where mobile = #{mobile}
        <if test="memberId != null">
            and member_id != #{memberId}
        </if>
    </select>

    <select id="checkEmailUnique" resultType="int">
        select count(*) from shop_member where email = #{email}
        <if test="memberId != null">
            and member_id != #{memberId}
        </if>
    </select>

</mapper>
